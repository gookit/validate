# v2.0 Issue 核查与处理交接

> **目的**：核查 gookit/validate 的 open issues 在 v2.0（master 分支）下是否已解决，复现测试写入 `issues_x2_test.go`。
> **状态**：已核查 17 个（第一批 8 + 第二批 8 + #184）。**已修 bug 7 个**：第一批 #314 `d1031d6`、#217 `da6eda7`、#262 `6e600ed`、#283 `1fdb557`；第二批 #138 `e1e79ef`（URL 正则）、#265 `bd0dc60`（CheckZero 标 Deprecated）、#324 `bbdad99`+`a9c3fd3`（嵌套表单绑定，对象嵌套）。**新增 feature 1 个**：#184（错误消息追加触发值，opt-in `ErrShowValue`，默认关）。第二批其余：#277/#162/#203/#244 answered、#266 partial。剩余 5 个为纯特性/文档类。
> **日期**：2026-06-04（更新于 2026-06-05）
> **环境**：master 已是 v2.0（`module github.com/gookit/validate/v2`，go 1.21；本地用 go1.25.10，xenv 自动激活）。`gh` 未登录 → 用公开 API 拉 issue：`curl -s "https://api.github.com/repos/gookit/validate/issues/<N>" -H "Accept: application/vnd.github+json" | jq -r '.title, .body'`。

---

## 1. 第一批已核查（8 个，测试在 `issues_x2_test.go`，全量 `go test -count=1 .` 绿）

测试写法约定：断言**当前 v2.0 真实行为**，用描述性子测试名标注 resolved / still-broken，**不留红测试**（still-broken 也是 PASS，只是断言"错误的现状"并在名字/注释标明）。

| # | 标题 | v2.0 结论 | 根因 / 解法 | 测试函数 |
|---|---|---|---|---|
| **#259** | Embedded structs not validated properly | ✅ **已修** | 匿名嵌入现始终级联、字段被验证（T10 豁免）。报告里 perm2 仍报错是设计行为：内层 `required` 独立求值，不被外层 `required_if` 门控 | `TestIssue_259_v2` |
| **#232** | struct 方法验证 slice-of-struct B | ✅ **已解** | 给 slice 字段加 `validate` tag（空 tag `validate:""` 也算）即级联每个 B 元素的字段规则（`CheckSubOnParentMarked` 行为）——这就是提问的答案 | `TestIssue_232_v2` |
| #327 | 未指定 required 时取不到字段值 | ✅ 非 bug（设计） | 只有"参与校验且通过"的字段进 safeData / 可被 BindStruct 绑定；非 required 规则 + 非空值能正常绑定；完全无规则字段才取不到。`AddMessages` 不写 required 项不影响其它字段消息 | `TestIssue_327_v2` |
| #235 | 如何验证 struct 内的 slice-of-struct | 🟡 partial | 自定义验证器 workaround 在 v2.0 完全可用（需给字段加 validate tag 触发级联）；路径式 `required_with:Nodes..Location` 跨元素引用仍失效 | `TestIssue_235_v2` |
| **#217** | Nested resources evaluated differently | ✅ **已修**（`da6eda7`） | 切片元素里 `*bool→false` 曾被 `required` 误判为空：`data_source.go` 子结构体导航循环把叶子指针也解引用成 `bool(false)`（`IsEmpty==true`）。修复让叶子节点保留非 nil 指针（与顶层路径一致），中间节点仍解引用导航 | `TestIssue_217_v2` |
| **#283** | Scenes does not work in slices | ✅ **已修**（`1fdb557`，仅 `.*`） | 场景通配 `Tags.*.Id` 现可命中级联生成的 `Tags.0.Id`：`sceneFieldMap` 把含 `.*` 的项收进 `sceneWildcards`，`isNotNeedToCheck` 把字段名数字索引段规范化为 `*` 再比对。无索引 `Tags.Id` 因歧义按设计不做 | `TestIssue_283_v2` |
| **#314** | Scene with empty validation rules now fails (回退) | ✅ **已修**（`d1031d6`） | `"None": []string{""}` 本应"不校验任何字段"，但 `sceneFieldMap()` 把 `""` 当普通键，`isNotNeedToCheck` 里 `strings.Join(fields[0:0],".")` 得 `""` 恰好命中。修复：`sceneFieldMap` 跳过空字段但保留非 nil map；`isNotNeedToCheck` 用 nil/非nil 区分"未设场景(校验全部)"与"场景激活但无字段(都不校验)" | `TestIssue_314_v2` |
| **#262** | filter 无法对切片中元素应用 | ✅ **已修**（`6e600ed`） | `FilterRule.Apply` 对**内部**通配（`.*` 后还有字段）逐元素 apply filter，结果写回 `filteredData[通配key]` 与验证器衔接；**末尾** `.*`（如 `Domains.*`）仍走整切片以兼容 `trimStrings` 等切片级 filter（#172）。范围：Map/JSON + 单级 `.*` | `TestIssue_262_v2` |

### still-broken 的修复落点（原 4 个，**全部已修**）

- ~~**#314**：`sceneFieldMap()` 跳过空字段（保留非 nil map）；`isNotNeedToCheck()` 用 nil/非nil 区分"未设场景/场景无字段"。~~ ✅ `d1031d6`
- ~~**#217**：`data_source.go` 子结构体导航循环对**叶子节点保留非 nil 指针**（中间节点仍解引用），与顶层路径对齐。~~ ✅ `da6eda7`
- ~~**#262**：`FilterRule.Apply` 对**内部** `.*` 通配逐元素 apply（抽出 `applyFilters`）；末尾 `.*` 保持整切片以兼容 `trimStrings`（#172）。~~ ✅ `6e600ed`
- ~~**#283**：`sceneFieldMap` 分出 `sceneWildcards`；`isNotNeedToCheck` 用 `indexPathToWildcard` 规范化数字索引段后比对通配集。仅 `.*` 通配。~~ ✅ `1fdb557`

设计稿见 `v2.0-283-262-wildcard-design.md`。范围决策（已落地）：#262 先 Map/JSON + 单级 `.*`；#283 仅 `.*` 通配、不做无索引写法。**后续可扩展**：#262 struct 源逐元素写回、多级 `.*`；#283 无索引/前缀通配（需先解歧义）。

**注意**：每个 fix 均已跑 golden `go test -run TestRuleCompat -count=3 .`（逐字节）+ 全量 + race + vet 全绿，并把对应 `TestIssue_<N>_v2` 断言从 broken 改回 correct。

---

## 2. 剩余待核查的 open issues（共 22 个，已核查 17 个）

> 拉取方式：`curl -s "https://api.github.com/repos/gookit/validate/issues?state=open&per_page=60" -H "Accept: application/vnd.github+json" | jq -r '.[] | select(.pull_request==null) | "#\(.number)\t\(.title)"'`

**已核查（第二批，测试在 `issues_x2_test.go`）**：

| # | 标题 | v2.0 结论 | 根因 / 修复方向 / 答案 | 测试函数 |
|---|---|---|---|---|
| **#138** | FullUrl regex needs improvement | ✅ **已修**（`e1e79ef`） | `FullURL` 改为 scheme + 结构化 host（domain.tld 或 IPv4）+ 可选 port + path/query，拒绝 issue 三个反例（无 TLD、`%23`、host 含 `_`）；`IsURL` 保持宽松 URI-ref 语义、仅补空白字符护栏让 `"not a url"` 被拒。现有 `TestIsFullURL`/`TestURLString` 无回归 | `TestIssue_138_v2` |
| **#324** | Nested Form Data Binding Fails (multipart) | ✅ **已修**（`bbdad99`+`a9c3fd3`，仅对象嵌套） | ① `FromURLValues` 用 `normalizeFormKey` 把 bracket 归一为点路径（`address[street]`→`address.street`）；② `BindSafeData` 用 `maputil.SetByPath` 把点路径 safeData 展开成嵌套 map 再绑定。前提：nested 字段需点路径规则才进 safeData。**数组 bracket（`items[0][name]`）经探针验证暂不做**（maputil 建不出 slice + 表单 `.*` 无法枚举，详见设计稿 §4.1） | `TestIssue_324_v2` |
| **#277** | Identifying the First Failed Field | ✅ **answered（非 bug）** | `StopOnError=true`（默认）失败后 `v.Errors` 仅含第一个出错字段；`v.Errors.Field(f)` 取 `map[validator]message`、`FieldOne(f)` 取消息。即提问答案 | `TestIssue_277_v2` |
| **#266** | `in` 能否用于 slice tag 校验 | 🟡 **partial（用 `.*` 程序化）** | tag 里 `in`(enum) 把整个 `[]string` 当单值 → 失败；逐元素须用程序化 `S.*` + `StringRule`，tag 方式不支持 | `TestIssue_266_v2` |
| **#265** | CheckZero config flag 怎么用 | ✅ **已处理（标 Deprecated）** | `GlobalOption.CheckZero` 自声明起即 no-op；已标 `Deprecated`（`bd0dc60`），注释指向替代手段 `Rule.SetSkipEmpty(false)` / `SkipOnEmpty=false` / `required`，后续移除。零值校验与 CheckZero 无关 | `TestIssue_265_v2` |
| **#162** | required_if 与校验规则组合 | ✅ **answered（可实现）** | `required_if:Type,B\|uuid4`：Type==B 时 ID 必填+uuid4；Type!=B 时空值跳过、非空值仍按 uuid4 校验（uuid4 不被条件门控，仅"是否必填"被门控） | `TestIssue_162_v2` |
| **#203** | 解释 required 如何工作 | ✅ **answered（非 bug）** | 评论楼最后"去掉 required 后 filter/validator 不跑、非法被放过"系对 SkipOnEmpty 的误解：非 required 字段对**非空**值 filter/validator 照跑、非法被拒；仅**空值**按 SkipOnEmpty 跳过（可选未填）。要拒空加 `required`。另原始 `required\|int` 对 `Name=4` 误报已在 v2.0 修复 | `TestIssue_203_v2` |
| **#244** | sanitize all data with bluemonday（用法/集成） | ✅ **answered（用法）** | 想在绑定到 struct 前用 bluemonday 清掉字符串里的 HTML/JS：用本库自带自定义 filter 机制即可，filter 在校验/绑定**前**运行 —— 把 `policy.Sanitize` 包成 filter，挂到字符串字段（`filter:"sanitizeHTML"` tag 或程序化 `FilterRule`），再 `BindSafeData`。**无需新依赖、无需改库**。测试用最小 stand-in 去标签函数演示，真实场景换成 `bluemondayPolicy.Sanitize` | `TestIssue_244_v2` |
| **#184** | Improve error messages: add original value | ✅ **feat 已实现（opt-in）** | 新增开关 `GlobalOption.ErrShowValue`（默认 `false`），按现有模式拷贝到 `Validation` 实例（`newEmpty`/`resetForReuse`）。**默认关：错误消息逐字节不变**（golden `TestRuleCompat` ×3 + 精确字符串断言均无回归）；**开启后**在 `validating.go applyField` 失败分支把触发值以 ` (value: <val>)` 追加（`val` 为 filter/类型转换后的实际触发值）。用法：`Config(opt.ErrShowValue=true)` + `defer ResetOption()`。测试在新文件 `issues_x3_test.go`（x2 接近 1000 行上限） | `TestIssue_184_v2` |

**未处理 — 特性请求/文档类（非 bug，无可复现行为，按需）**：
- #292 支持完整的逻辑条件（特性）
- #257 Enhancement: Add IsActiveURL Validation Rule（特性）
- #215 Doc: Benchmark（文档）
- #189 希望增加 StringMessage（特性）
- #11 validate.Errors 希望增加 trans 功能（特性）
- #88 V2 - refactoring（即本次 v2.0 工作，可关）

---

## 3. /clear 后如何继续

1. 工作目录 `/d/work/inhere/my-tools-dev/gookit2/validate`，分支 **master**（已是 v2.0）。`go version` 确认 go1.25.10，否则 `export GOROOT=/d/work/inhere/linux-env/sdk/gosdk/go1.25.10; export PATH=$GOROOT/bin:$PATH; export GOTOOLCHAIN=local`。**跑基准须加 `-run='^$'`**（预存非幂等 `TestBuiltinMessages`）。
2. 已写的 issue 测试在 `issues_x2_test.go`，函数名 `TestIssue_<N>_v2`。跑：`go test -run 'TestIssue_.*_v2' -v .`。
3. **若要修 still-broken bug**：从 §1 的"修复落点"入手（#314/#217 优先），改完把测试断言从 broken 改回 correct，跑 golden + 全量 + race。
4. **若要继续核查剩余 issue**：从 §2 的 #324/#138 等可测项开始，公开 API 拉正文，复现到 `issues_x2_test.go`。
5. v2.0 完整实施记录见 `docs/plans/v2.0-implementation-plan.md`（T0–T10 对账）。
