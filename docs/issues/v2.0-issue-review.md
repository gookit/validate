# v2.0 Issue 核查与处理交接

> **目的**：核查 gookit/validate 的 open issues 在 v2.0（master 分支）下是否已解决，复现测试写入 `issues_x2_test.go`。
> **状态**：第一批 8 个已核查（master，3 提交 `d9364fc`/`e57a0a4`/`fdfd0e6`）。still-broken 4 个中 **#314 / #217 已修复**（`d1031d6`/`da6eda7`），剩 #283 / #262 待定设计；剩余 issue 待续。
> **日期**：2026-06-04
> **环境**：master 已是 v2.0（`module github.com/gookit/validate/v2`，go 1.21；本地用 go1.25.10，xenv 自动激活）。`gh` 未登录 → 用公开 API 拉 issue：`curl -s "https://api.github.com/repos/gookit/validate/issues/<N>" -H "Accept: application/vnd.github+json" | jq -r '.title, .body'`。

---

## 1. 第一批已核查（8 个，测试在 `issues_x2_test.go`，全量 `go test -count=1 .` 绿）

测试写法约定：断言**当前 v2.0 真实行为**，用描述性子测试名标注 resolved / still-broken，**不留红测试**（still-broken 也是 PASS，只是断言"错误的现状"并在名字/注释标明）。

| # | 标题 | v2.0 结论 | 根因 / 解法 | 测试函数 |
|---|---|---|---|---|
| **#259** | Embedded structs not validated properly | ✅ **已修** | 匿名嵌入现始终级联、字段被验证（T10 豁免）。报告里 perm2 仍报错是设计行为：内层 `required` 独立求值，不被外层 `required_if` 门控 | `TestIssue_259_v2` |
| **#232** | struct 方法验证 slice-of-struct B | ✅ **已解** | 给 slice 字段加 `validate` tag（空 tag `validate:""` 也算）即级联每个 B 元素的字段规则（`CheckSubOnParentMarked` 行为）——这就是提问的答案 | `TestIssue_232_v2` |
| #327 | 未指定 required 时取不到字段值 | ✅ 非 bug（设计） | 只有"参与校验且通过"的字段进 safeData / 可被 BindStruct 绑定；非 required 规则 + 非空值能正常绑定；完全无规则字段才取不到。`AddMessages` 不写 required 项不影响其它字段消息 | `TestIssue_327_v2` |
| #235 | 如何验证 struct 内的 slice-of-struct | 🟡 partial | 自定义验证器 workaround 在 v2.0 完全可用（需给字段加 validate tag 触发级联）；路径式 `required_with:Nodes..Location` 跨元素引用仍失效 | `TestIssue_235_v2` |
| **#217** | Nested resources evaluated differently | ✅ **已修**（`da6eda7`） | 切片元素里 `*bool→false` 曾被 `required` 误判为空：`data_source.go` 子结构体导航循环把叶子指针也解引用成 `bool(false)`（`IsEmpty==true`）。修复让叶子节点保留非 nil 指针（与顶层路径一致），中间节点仍解引用导航 | `TestIssue_217_v2` |
| **#283** | Scenes does not work in slices | ❌ **仍 bug（需改代码）** | 场景字段名 `Tags.Id` / 通配 `Tags.*.Id` 命不中级联生成的规则名 `Tags.0.Id`（场景按字符串**精确**匹配）；只有显式数字索引 `Tags.0.Id` 能命中 | `TestIssue_283_v2` |
| **#314** | Scene with empty validation rules now fails (回退) | ✅ **已修**（`d1031d6`） | `"None": []string{""}` 本应"不校验任何字段"，但 `sceneFieldMap()` 把 `""` 当普通键，`isNotNeedToCheck` 里 `strings.Join(fields[0:0],".")` 得 `""` 恰好命中。修复：`sceneFieldMap` 跳过空字段但保留非 nil map；`isNotNeedToCheck` 用 nil/非nil 区分"未设场景(校验全部)"与"场景激活但无字段(都不校验)" | `TestIssue_314_v2` |
| **#262** | filter 无法对切片中元素应用 | ❌ **仍 bug（需改代码）** | 通配路径 `ports.*.container_start` 经 GetByPath 取到整个切片 `[]any{80}`，filter("int") 把 slice 当单值转 int → `convert value type error`；显式索引 `ports.0.container_start` 正常 | `TestIssue_262_v2` |

### still-broken 的修复落点（原 4 个，已修 2 个，剩 2 个）

- ~~**#314**：`sceneFieldMap()` 跳过空字段（保留非 nil map）；`isNotNeedToCheck()` 用 nil/非nil 区分"未设场景/场景无字段"。~~ ✅ 已修 `d1031d6`
- ~~**#217**：`data_source.go` 子结构体导航循环对**叶子节点保留非 nil 指针**（中间节点仍解引用），与顶层路径对齐。~~ ✅ 已修 `da6eda7`
- **#283**（面较大，先定设计）：场景匹配需支持**通配/前缀**以命中切片元素字段（`Tags.*.Id` → `Tags.0.Id`...）。
- **#262**（面较大，先定设计）：filter 阶段需对**通配路径展开**并逐元素应用（与验证器的 `.*` 展开逻辑类似）。

**剩余两个**（#283 / #262）都涉及**通配路径展开**，建议先定设计再实施（可能共用一套 `.*` 展开/逐元素应用的基础设施）。
**注意**：修这些时务必跑 golden `go test -run TestRuleCompat -count=3 .`（逐字节）+ 全量 + race；并把对应 `TestIssue_<N>_v2` 的断言从"现状(broken)"改回"期望(correct)"。

---

## 2. 剩余待核查的 open issues（共 22 个，已处理 8 个）

> 拉取方式：`curl -s "https://api.github.com/repos/gookit/validate/issues?state=open&per_page=60" -H "Accept: application/vnd.github+json" | jq -r '.[] | select(.pull_request==null) | "#\(.number)\t\(.title)"'`

**未处理 — 可测的 bug / 行为类（优先）**：
- #324 Nested Form Data Binding Fails for multipart/form-data Requests
- #138 FullUrl regex needs improvement

**未处理 — 问题/特性请求/文档类（多数非 bug，按需）**：
- #292 支持完整的逻辑条件（特性）
- #277 Identifying the First Failed Field（StopOnError=true，问题）
- #266 [Question] Can `in` be used with slice in tag validation?
- #265 [Question] How CheckZero config flag works.
- #257 Enhancement: Add IsActiveURL Validation Rule（特性）
- #244 [Question] sanitize all data with bluemonday
- #215 Doc: Benchmark（文档）
- #203 An explanation of how `required` works.（文档/问题）
- #189 希望增加 StringMessage（特性）
- #184 Improve error messages: add original value（特性）
- #162 Combine required_if with a validation rule（问题，与 #259 相关）
- #11 validate.Errors 希望增加 trans 功能（特性）
- #88 V2 - refactoring（即本次 v2.0 工作，可关）

---

## 3. /clear 后如何继续

1. 工作目录 `/d/work/inhere/my-tools-dev/gookit2/validate`，分支 **master**（已是 v2.0）。`go version` 确认 go1.25.10，否则 `export GOROOT=/d/work/inhere/linux-env/sdk/gosdk/go1.25.10; export PATH=$GOROOT/bin:$PATH; export GOTOOLCHAIN=local`。**跑基准须加 `-run='^$'`**（预存非幂等 `TestBuiltinMessages`）。
2. 已写的 issue 测试在 `issues_x2_test.go`，函数名 `TestIssue_<N>_v2`。跑：`go test -run 'TestIssue_.*_v2' -v .`。
3. **若要修 still-broken bug**：从 §1 的"修复落点"入手（#314/#217 优先），改完把测试断言从 broken 改回 correct，跑 golden + 全量 + race。
4. **若要继续核查剩余 issue**：从 §2 的 #324/#138 等可测项开始，公开 API 拉正文，复现到 `issues_x2_test.go`。
5. v2.0 完整实施记录见 `docs/plans/v2.0-implementation-plan.md`（T0–T10 对账）。
