# validate v1.6.0 实施计划

> **目标版本**: v1.6.0（基于 v1.5.7）
> **性质**: 保持公共 API 兼容的内部性能重构（升级零改动，只是更快）
> **设计依据**: `docs/validate-v2-design.md`（rev.3，§6.1 P0–P6）
> **Go 版本**: 维持 `go 1.19` 不变
> **状态**: 待执行
> **创建**: 2026-06-03

---

## 0. 总览

把 `docs/validate-v2-design.md` §6.1 的 P0–P6 落地为 v1.6.0。核心三件事：
1. **TypeMeta**：缓存 struct 类型级反射元数据（字段索引、tag、预解析规则模板）。
2. **FieldValue**：贯穿管线、携带 `reflect.Value` 的值载体，消除 `reflect.Value→Interface()→reflect.ValueOf` 往返。
3. **管线拆分 + 实现下沉 internal**，提升可维护性。

**红线（每阶段都必须满足）**：
- 现有全部测试零回归（`issues_test.go`/`issues_x2_test.go`/`validation_test.go`/`validators_test.go`/`data_source_test.go`/`morecase_test.go` 等）。
- 公共 API（包级函数、`Validation` 公共方法、`DataFace` 接口、内置验证器函数签名、struct tag 语义）**不变**。
- 基准不回退（以 P0 基线为准）。

> ⚠️ 破坏性内容（验证器统一 `reflect.Value` 入参、`DataFace` 扩展、注册自定义类型、`/v2`、升 Go 1.21）**不属于本计划**，留待 1.6.0 稳定后的 v2.0.0。见设计文档 §6.0。

---

## 1. 工作约定

### 1.1 构建 / 测试 / 基准命令
```bash
go build ./...
go vet ./...
go test ./...                              # 全量回归
go test -race ./...                        # 并发安全（P2/P5 必跑）
go test -run TestXxx -v ./...              # 单测
go test -bench=. -benchmem -count=3 .      # 基准（每阶段对比 P0 基线）
golangci-lint run                          # 提交前
```

### 1.2 提交规范（Conventional Commits + scope）
- 每个**子阶段完成**即提交一次，**不要攒到最后一次性提交**；子阶段改动多时按功能点再拆。
- 前缀：`feat / fix / refactor / perf / docs / test / chore`，带 scope。
- 示例：
  - `test(bench): add baseline benchmarks for v1.6.0 perf work`
  - `perf(fieldval): introduce FieldValue to remove reflect round-trip`
  - `refactor(meta): cache struct type metadata via sync.Map`
  - `perf(rule): pre-parse rule templates at type-build time`
- > 注：当前 `validate/` 目录无 `.git`（不是独立仓库）。若实际仓库在上层或后续 `git init`，按上述规范提交；否则以"完成子阶段即更新本文档 checkbox"为同等节点。

### 1.3 本文档维护
- 每完成一个任务勾选对应 `[ ]` → `[x]`。
- 每完成一个阶段，在该阶段「验收门禁」记录实测数据 / 结论。
- P0 的基线数据填入 §3.P0 的表格，后续阶段对照填写。

### 1.4 文件行数约束（AGENTS.md：单文件 ≤ 1000 行）
- 现状超标：`data_source.go`(1031)、`validators.go`(1305)。
- 本重构会把 `StructData` 的规则收集逻辑拆出（P3/P4），**顺带让 `data_source.go` 回落到 1000 行内**。
- `validators.go` 拆分不在本计划范围（属独立 cleanup）。新增代码避免继续堆进这两个文件。

---

## 2. 阶段依赖与顺序

```
P0 基线 ──► P1 FieldValue ──► P2 TypeMeta ──► P3 RuleTpl ──► P4 管线拆分+下沉internal ──► P5 可选Factory/Pool ──► P6 收尾
            (低风险)          (中)            (中)           (中)                        (低)              (低)
```
- 严格顺序执行：每阶段以"全量测试绿 + 基准不回退"为进入下一阶段的前提。
- P1/P2/P3 在**根包内**完成（避免 import 环）；P4 再物理迁入 `internal/`。

---

## 3. 阶段任务清单

### P0 — 基线基准 🟢低风险

**目标**：建立可量化的性能基线，作为后续每阶段的不回退门禁。

**任务**
- [x] 扩充 `benchmark_test.go`，新增以下基准（均带子结构 / 真实数据，使用 `b.ResetTimer()`；**每次迭代构造全新 Validation**，避免现有 `BenchmarkFieldSuccess` 的 reuse-instance 短路陷阱）：
  - [x] `BenchmarkStructFlat`：3 字段扁平 struct（`required|minLen` / `email` / `int|min|max`），重复验证同类型
  - [x] `BenchmarkStructNested`：struct 套 struct（2 层）
  - [x] `BenchmarkStructSliceOfStruct`：含 `[]Sub` 且 Sub 带规则（3 元素）
  - [x] `BenchmarkMapValidate`：Map 数据源（应持平，作对照组）
  - [x] `BenchmarkValValue`：`Val("john@example.com","required|email")`
  - [x] `BenchmarkStructFirstParse`：每次 `Struct(&u)` 仅构造+解析、不调 Validate，测纯解析成本
- [x] 运行 `go test -bench=. -benchmem -count=3 .`，结果填入下方基线表 + 存档 `docs/plans/p0-baseline.txt`
- [ ] 安装/确认 `benchstat`（后续阶段用 `benchstat p0-baseline.txt new.txt` 做显著性对比）—— 待 P1 用时按需 `go install golang.org/x/perf/cmd/benchstat@latest`

**基线数据**（2026-06-03，go1.25.10，i7-14700KF，count=3 代表值；原始输出见 `docs/plans/p0-baseline.txt`）

| 基准 | ns/op | B/op | allocs/op |
|---|---|---|---|
| BenchmarkStructFlat | 10403 | 18353 | 132 |
| BenchmarkStructNested | 10128 | 17936 | 132 |
| BenchmarkStructSliceOfStruct | 17249 | 24062 | 260 |
| BenchmarkMapValidate | 9606 | 17918 | 124 |
| BenchmarkValValue | 716.7 | 495 | 11 |
| BenchmarkStructFirstParse（仅构造+解析） | 8580 | 17392 | 125 |

> 🔑 **关键发现**：`StructFirstParse`（仅构造+解析）= 17392 B/op / 125 allocs，而完整 `StructFlat` = 18353 B/op / 132 allocs ——**约 95% 的内存分配发生在"构造 + parseRulesFromTag"阶段，验证本身只多 ~7 allocs**。印证设计判断：**P2(TypeMeta) / P3(RuleTpl) 缓存是最大收益点**，应作为优化重心。

**验收门禁**：✅ 基线数据已存档；`benchmark_test.go` 编译通过、基准可运行（监督者复跑数字稳定）。
**提交**：`test(bench): add baseline benchmarks for v1.6.0 perf work`

---

### P1 — FieldValue 值载体 🟢低风险

**目标**：引入携带 `reflect.Value` 的值载体，先在热点函数内部替换重复 `reflect.ValueOf`，**对外行为完全不变**。

**关键设计（见设计 §4.3）**
```go
// 新文件：fieldvalue.go（根包；P4 再考虑迁 internal）
type FieldValue struct {
    name string
    src  any
    rv   reflect.Value  // 懒：reflect.ValueOf(src)；若来源已有 reflect.Value 则直接塞入
    rt   reflect.Type   // 懒
    real reflect.Value  // 懒：reflect.Indirect(rv)
    rvInit, realInit bool
    zero, empty int8    // 0未知 1是 2否（懒）
}
func (f *FieldValue) RV() reflect.Value   // 懒构造并缓存
func (f *FieldValue) Real() reflect.Value // 去指针，统一指针语义
func (f *FieldValue) IsZero() bool        // 懒
func (f *FieldValue) IsEmpty() bool       // 懒，复用 RV()，替代 IsEmpty(any) 的再反射
```

**任务**
- [x] 新建 `fieldvalue.go`：不导出结构 `fieldValue` + `rV/rT/realV/isZero/isEmpty` 方法（懒求值、三态缓存）
  - 决策：**用不导出名 `fieldValue`**（不过早锁定公共 API；导出留待后续）
- [x] 提供两种构造：`newFieldValue(name, src)`（map/form 用）/ `newFieldValueRV(name, rv)`（struct 用，不调 `.Interface()` 还原；**P2 才接入**，P1 先定义补全 API）
- [x] 新建 `fieldvalue_test.go`：覆盖普通值/空串/`0`·`false`/nil·空 slice·map/`*T` 有效/`*T(nil)`/`any(nil)`/`**int`；`isEmpty` 对 18 输入逐一断言 `== IsEmpty(src)`（40+ 子用例全过）
- [x] 改造 `callValidatorValue`（validating.go:542）：新增末位参数 `vfv *fieldValue`，非 nil 时复用 `vfv.realV()` 替代第二次 `reflect.ValueOf(val)` 与指针解引用
- [x] 改造 `valueValidate`（validating.go:300）：复用 `fv.rV()`，并把载体经 `callValidator` 贯穿到 `callValidatorValue`
- [x] 指针/零值语义集中到 `fieldValue.realV/isEmpty`（保留公共函数不动）

**注意 / 坑（已落实）**
- `IsEmpty(any)`（validators.go:542）签名不动；`fieldValue.isEmpty()` 复用 `ValueIsEmpty`（util.go:185），三路径与 `IsEmpty(any)` 对齐。
- typed-nil vs untyped-nil：`rV()` 对 `src==any(nil)` 返回 `nilRVal`；`valueValidate` 中 `src==nil` 时强制 `valKind=Invalid` 还原原行为（`rftVal` 仅 Slice 分支消费，nil 永不进入）。
- **行为等价铁律**：任何"值与载体不一致"的点（类型转换后、slice 子元素、nil）一律传 `vfv=nil` 回退原 reflect 逻辑。监督者逐行核对 diff，确认 100% 等价。

**验收门禁**：✅ 全量测试 + `-race` 全绿；基准全部持平或微降（StructFlat ~-2.3%），**无回退**。
- ⚠️ **门禁修正（真实发现）**：原预期"StructFlat allocs 下降"**不成立且非回退**——P0 剖析已知 allocs 集中在解析阶段；且所有基准 struct 用**内置**验证器，命中 `callValidator` switch 快路径，**根本不走 `callValidatorValue` 反射路径**，故 P1 的反射去重对这些基准无 alloc 影响。P1 的反射去重仅惠及**自定义验证器**路径（正确性由海量回归用例保证），其 alloc 收益需自定义验证器基准才能体现（可于 P4 补）。P1 的核心交付是**抽象层落地 + 为 P2 预留 `newFieldValueRV`**。
**提交**：`perf(fieldval): introduce fieldValue carrier to dedup reflect in validate path`

---

### P2 — 类型元数据缓存 TypeMeta 🟡中风险

**目标**：同一 struct 类型的字段索引 / kind / tag 只构建一次并缓存；字段访问由 `FieldByName`（O(n)）改 `FieldByIndex`（O(1)）。

**关键设计（见设计 §4.2 / §4.6）**
```go
// cache.go（替换现有全注释内容）
type FieldMeta struct {
    Index   []int        // FieldByIndex 路径
    Name, Path string
    Kind   reflect.Kind  // 去指针后
    IsPtr  bool
    Elem   elemClass     // leaf / structKind / sliceOfStruct / mapOfStruct...
    ValidateRule, FilterRule, OutputName, Label string
    Messages map[string]string
}
type TypeMeta struct {
    Type   reflect.Type
    Fields []*FieldMeta
    byName map[string]*FieldMeta
    // P3 再填充：StaticRules / DynamicFields
    Labels, Outputs map[string]string
    ImplConfig, ImplTranslates, ImplMessages bool
}
type typeKey struct { rt reflect.Type; tagVer uint32 }
var typeCache sync.Map  // map[typeKey]*TypeMeta
func getTypeMeta(rt reflect.Type) *TypeMeta  // 命中即返回；未命中构建后存入
```

**任务**
- [x] `cache.go`(228行)：`fieldMeta`/`typeMeta`/`typeKey` + `getTypeMeta`(Load→build→LoadOrStore) + `buildTypeMeta`（**纯类型遍历 `t.Field(i)`，不碰任何值**；递归 struct-of-struct，跳过 `timeType`/非导出；slice·map-of-struct 仅标记入 `dynamicFields` 不展开；5 个 tag 一次性 `Tag.Get` 存 `fieldMeta` 供 P3 用）
- [x] tag 配置版本：`Config()`/`ResetOption()` 末尾 `atomic.AddUint32(&tagVer,1)`；`getTypeMeta` 用 `typeKey{rt,tagVer}` → 配置变更旧缓存自然不可达
- [x] 公共 `ResetTypeCache()`（`typeCache.Range`+`Delete`，并发安全）
- [x] `StructData` 加 `meta *typeMeta`；`FromStruct` `data.meta = getTypeMeta(typ)`
- [x] `TryGet` **仅顶层导出字段**经 `topFieldMeta` 命中后用 `FieldByIndex`，否则回退 `FieldByName`；嵌套/slice/map 路径不动
- [x] `Create` 三处 `Implements` 改读 `d.implFaces()`（meta 缺失回退 live 检查，行为等价）
- [x] `cache_test.go`：命中同实例 / `*T`·`T` 共享 / 嵌套 Index / 非导出(默认跳过·开启后含) / Implements / tagVer 失效 / ResetTypeCache / 50 并发 `getTypeMeta`(`-race`) **+ 递归类型回归(见下)**

**坑（已落实）**
- 非导出/匿名字段：`topFieldMeta` 仅对"meta 命中 + 单级 Index + 首字母大写"用 `FieldByIndex`，其余回退 `FieldByName`（规避非导出 panic）。
- **🐞 监督者修复的真实回归（agent 漏掉）**：`buildTypeMeta` 走**类型树**，对自引用/互递归 struct 类型（`type Node struct{Next *Node}`）**无限递归→栈溢出**（原 `parseRulesFromTag` 走值树，nil 指针天然终止，故原来正常）。**修复**：`walk` 加**祖先链环检测**（同类型在兄弟分支仍可展开，只切断祖先环；环上 struct 字段记入 `dynamicFields` 交 P3 按值处理）。已加 `TestGetTypeMeta_cyclicType`（self-ref / mutual-rec / 端到端不挂起）永久锁定。
- `Set` 路径**保守未改**（仍 `FieldByName`，含 nil 指针初始化，按"有不确定就不动"）。
- `timeType` 已在 build 递归中排除。

**验收门禁**：✅ 全量测试 + `-race` 全绿（含递归类型回归）。
- ns/op：StructFlat -3.7% / Nested -3.1% / Slice -1.5%（FieldByIndex + 省 3 次 Implements）；MapValidate -2.4%；allocs **全部持平**（parseRulesFromTag 未动，符合预期——big win 在 P3）。
- ⚠️ **门禁修正**：原"ns/op 显著下降"对 P2 不成立——P2 是基础设施 + 字段访问，规则解析缓存（真正大头）在 P3。P2 达成"infra 正确 + 字段访问等价 + 不回退"即合格。
- ℹ️ `data_source.go` 1031→1078 行（已超 1000 软限，P2 前即超标）；按计划 §P3 拆出规则收集时一并回落 ≤1000。

**提交**：`refactor(meta): add cached type metadata + FieldByIndex access + cycle-safe build`

---

### P3 — 规则模板缓存 RuleTpl + build/instantiate 两段式 🟡中风险

**目标**：把规则收集拆成"类型级构建（缓存）"+"值级实例化"，并把规则字符串解析、别名解析、参数类型转换前移到构建期（最大增益点之一）。

**关键设计（见设计 §4.4）**
```go
type RuleTpl struct {
    Path, Validator, RealName string
    NotRequired bool
    Args        []any      // 构建期已按验证器签名转换好，运行时不可变
    FuncMeta    *funcMeta  // 全局/内置验证器构建期绑定；struct 方法验证器留空、运行时解析
    FilterRule  string
}
// TypeMeta 补充：
//   StaticRules   []*RuleTpl   // 路径恒定（leaf、struct-of-struct）
//   DynamicFields []*FieldMeta // slice/map-of-struct，需按值展开
```
拆分 `parseRulesFromTag`（data_source.go:270）：
- `build(rt)`：只遍历**类型**，解析全部 tag → 生成 `StaticRules` + 翻译表；slice/map-of-struct 记入 `DynamicFields`（递归缓存元素类型 meta）。
- `instantiate(value, v)`：挂载 `StaticRules`（零反射）；仅对 `DynamicFields` 按真实值展开元素路径（`items.0.name`...）。

> **实现决策（比原设计更安全）**：放弃"构建期预转换 args + 全量静态展开"（`%#v` 黄金测试无法验证 arg 类型转换且涉及自定义验证器/nil 语义风险）。改为 **静态快路径 + 动态回退**：
> - **静态类型**（字段图沿非指针 struct 递归无 ptr-to-struct / slice·map-of-struct）：规则集与值无关 → 用**零值实例跑一次现有 `parseRulesFromTag` 生成模板快照**并缓存（复用同一解析代码 ⇒ 保证逐字节一致），实例化时克隆模板（args 逐实例拷贝、保留 nil 语义、trans replay）。
> - **动态类型**（ptr/slice/map-of-struct、递归类型）：**完全走原 `parseRulesFromTag` 不变**，零风险。
> - args 仍保持收集期形态（字符串），运行时 `convertArgsType` 不变（"构建期预转换 + 共享不可变 rule"留待 P5/v2，因 newEmpty 才是残余分配大头）。

**任务（已完成）**
- [x] P3a：`regression_compat_test.go` 黄金安全网（27 用例，`%#v` 含 arg 类型；钉死 enum/notIn 合并、regexp 不拆、nil-vs-empty args）—— **已独立提交 `48e2746`**
- [x] `struct_rules.go`(501行)：`computeIsStatic` 分类 + `buildRuleTemplate`(零值快照) + `staticTemplate`(sync.Once) + `instantiateStatic` + `cloneRule`/`cloneFilterRule`
- [x] `typeMeta` 加 `isStatic`/`tplOnce`/`tpl`；`buildTypeMeta` 末尾算 `isStatic`
- [x] `Create` 分发：`if d.meta.isStatic { instantiateStatic } else { parseRulesFromTag }`
- [x] 把 `parseRulesFromTag`、`loadMessagesFromTag` **逐字节移动**到 `struct_rules.go`（git 提取对比零 diff）→ `data_source.go` 1078→**838 行（≤1000）**
- [x] `cache_test.go` 加 `TestComputeIsStatic` + `TestStaticTemplate_concurrent/isolation`
- [x] 临时对照：20 个 static fixture 强制 static 路径 dump == dynamic 路径 dump 逐字节相等（已删临时测试）

**注意 / 坑（已落实）**
- **args 逐实例拷贝**：验证期 `convertArgsType` 原地改写 `r.arguments`，共享模板会污染/竞态 → `cloneRule` 每实例新建 args 切片；**保留 nil 语义**（模板 nil args 克隆为 nil，非空切片）。
- 模板懒构建 `sync.Once` 并发安全；模板只读不可变。
- 自定义/struct 方法验证器：static 模板由现有 `parseRulesFromTag` 生成，行为与原一致；含此类验证器的结构仍能正确静态化（解析逻辑不变）。
- 自引用/递归类型必含 ptr-to-struct → `computeIsStatic` 判 dynamic，不会进静态模板。

**验收门禁**：✅ **黄金测试 `count=3` 逐字节绿（硬门禁达成）**；全量 + `-race` 全绿；`data_source.go` 838 行。
- 基准（静态收益，动态/非struct 持平）：StructFlat **-19.7% ns / -34.8% allocs**(132→86)、StructNested **-20.7% ns / -34.8% allocs**、StructFirstParse **-24.4% ns / -36.8% allocs**(优于"≤基线+15%")；SliceOfStruct/Map/Val 持平（走原路径）。
- ⚠️ **未达 ≥40% 目标**（实测 ~35% allocs）：pprof 显示残余分配大头是 `newEmpty()` 的 `NewTranslator`（每实例拷贝 ~150 条 builtin messages + ~17 funcMeta）——这是 **per-Validation 构造成本，属 P5（Factory/pool + translator 共享 builtin）目标**。P0 的"95% 在 construct+parse"把两者混算，P3b 已砍掉 parse 半，construct 半（更大）待 P5。
**提交**：`perf(rule): cache static-type rule templates; move rule collection to struct_rules.go`

---

### P4 — 验证管线拆分（痛点 D）🟡中风险

> **范围决策（用户选定"只做 Part 1"）**：只做管线拆分（提升可维护性），**Part 2 下沉 internal 不做**——实测耦合度高（fieldValue/typeMeta 被 4+ 根文件用、底层原语散落 7 文件），需抽 `internal/reflectx` 破 import 环、牵动 7+ 文件，纯结构搬家无功能/性能收益，性价比低。

**任务（已完成，仅改 validating.go）**
- [x] `Rule.Apply`（85-192）拆为：薄 `Apply`（scene/beforeFunc 守卫 + per-field 循环）+ `applyField`（单字段全步骤；原每个 `continue`→`return false`、`return true` 原样，逐分支等价）
- [x] `valueValidate` 拆出 `validateWildcardSlice`（`.*` 通配符 slice 分支，~55 行最纠缠的冷路径逻辑原样平移）；safe/required/funcMeta/参数/标量路径保持线性
- [x] **过度拆分的回退（agent 良判）**：初版额外拆 `resolveFuncMeta`/`prepareFuncArgs`/`validateScalar`/`applyDefaultValue`/`applyFilterFunc`，但 `-gcflags=-m` 确认这些不被内联，热路径 +2.22% 回退 → 回退为 inline，只留两个零成本提取
- [x] ~~下沉 internal~~（Part 2，按决策不做）

**验收门禁**：✅ golden(count=3) 逐字节绿 + 全量 + `-race` 绿；基准 +0.6%/Flat、+0.4%/Nested、Map 持平（**门内 ±3%，allocs 全不变**）；`validating.go` 692→718 行（≤1000）。
- 微权衡：`applyField` 调用帧带来 ~+0.6%（13ns/2307ns），相对已 banked 的 -78% 可忽略，换 Apply 瘦身 + 最纠缠的 wildcard 块独立。
**提交**：`refactor(exec): split Rule.Apply/valueValidate into single-responsibility steps`

---

### P5 — 削减 per-Validation 构造成本（newEmpty）🟢低风险

> **范围重定向（基于 P3b 剖析 + 用户决策"P5 优先抓性能"）**：原 P5 是 opt-in Factory/Pool，但实测 newEmpty=62 allocs 占 StructFlat 的 ~72%，且 Factory 是 opt-in、不影响默认基准。故 P5 重定向为**直接削减默认路径的 newEmpty 构造成本**（translator + context 验证器），这才是移动基准的真实杠杆。Factory/Pool + emptyV 并发改造**降级为可选 follow-up**（见 §P5-deferred）。

**P5a — Translator 共享 builtin 消息**（已完成）
- [x] `Reset()` 不再每实例拷贝 ~150 条 builtinMessages；`messages` 懒初始化只存 custom，查找回退全局 `builtinMessages`（`lookupMessage` helper，custom→builtin 顺序不变）
- [x] `HasMessage` 回退 builtin；`AddMessage(s)` 懒初始化；`findMessage` 4 处查找改走 helper
- [x] 3 个 locales 子包 + golden + race 全绿；`NewTranslator` allocs 13→≤3
- 提交：`perf(trans): share builtin messages instead of copying per Translator`（`a153155`）

**P5b — context 验证器懒构建**（已完成）
- [x] 包级静态绑定表 `ctxValidatorBuilders`（16 个，方法名差异 `isFile→IsFormFile`/`isImage→IsFormImage` 已处理）
- [x] `newEmpty` 删除急切构建 16 个绑定方法 funcMeta；`validatorMeta` 首次查到 context 名时懒构建并绑定真实 v、缓存
- [x] `HasValidator`/`Validators` 保持这 16 个始终可用（返回新 map，更安全）
- [x] 关键洞察:`required`(无`.*`)走 valueValidate 快路径,查 fm 前即返回 → 常见 struct 验证 **0 个** per-instance context meta
- [x] required-family 7 个确认全在 callValidator switch 直调(不经 fv.Call);eqField/file 走反射 Call → 绑定真实 v 保证正确
- [x] 新增 `TestAllocsNewEmpty`(≤8,实测 2)、`TestCtxValidatorLazyBuild`;未改现有测试
- 提交：`perf(validator): lazily build context validators (newEmpty 51->2 allocs)`

**验收门禁**：✅ 全量 + `-race` + golden(count=3) 全绿;`newEmpty` 62→**2** allocs。
- **监督者独立复测累计 P0→P5b**：StructFlat **10403→2308 ns(-78%)、132→34 allocs(-74%)**；Nested -79%/-74%；MapValidate **9606→3346 ns(-65%)、124→72(-42%)**（意外受益于 translator+ctx 共享）；SliceOfStruct -37%/-20%（动态，受益于构造共享）；Val 持平（emptyV 未动）。**远超设计 §7 达标线**（静态 ns↓≥50%、allocs↓≥40%）。

#### P5-deferred
- [x] **`Val()` 共享 `emptyV` 并发安全修复**（既有 latent bug：valueValidate 在参数类型错误时写 `emptyV.Errors`、P5b 后首次用 context 验证器写 `emptyV.validatorMetas` → DATA RACE / 可能 "concurrent map writes" fatal）。**修复**：改用 `sync.Pool` 提供每次调用独立实例 + 最小重置（清 Errors/hasError；懒建 validatorMetas 绑定该实例可复用）。`-race` 复现→修复前后对比验证；新增 `TestVal_concurrent`(64 goroutine,含 happy/error/context 三类路径)永久锁定。**ValValue 基准零回退**(726ns/11allocs ≈ P0)。提交：`fix(value): make Val/Var concurrency-safe via pooled validation`
- [ ] opt-in `Factory` + pooled `Validation` + `Release()`（不影响默认基准，heavy-reuse 场景用）—— 仍可选
- [ ] SliceOfStruct 动态路径每元素重复解析的进一步优化 —— 仍可选

---

### P6 — 收尾 🟢低风险

**目标**：文档、覆盖率、清单收口。

**任务**
- [x] 覆盖率 `go test -coverprofile`：**95.6%**（高；只增代码+测试，不降）
- [ ] 更新 `README.md`/`README.zh-CN.md`：**可选未做**——纯内部重构、零 API 变更，由 CHANGELOG 承载；28KB 双语文档改动留待需要时（见末尾"可选剩余"）
- [x] 更新 `CHANGELOG.md`：新增 `## v1.6.0` 段（perf + 两个 fix）；`docs/TODO.md` 勾掉 cache/field-value，标注 pool 部分完成、`notIn` 已存在
- [x] 本文档全部 checkbox 勾选完成，回填实测数据
- [x] 最终基准（count=6）存档 `docs/plans/p6-final.txt`，汇总见下
- [x] `gofmt` 干净、无死代码引入

**验收门禁**：✅ 覆盖率 95.6%；CHANGELOG/TODO/plan 更新完成；**整体基准远超设计 §7 达标线**（详见 §4）。
**提交**：`docs(v1.6.0): changelog, TODO, final benchmarks and plan completion`

---

## 4. 整体验收（v1.6.0）

- [x] P0–P3、P5（含 emptyV 修复）、P6 完成；**P4 按用户决策延后**（可维护性、性能中性、风险项，不阻塞发版）
- [x] `go test ./...` 全绿；`go test -race ./...` 全绿
- [x] `go vet ./...` 通过（`golangci-lint` 未安装，用 vet 兜底）
- [x] 公共 API 无变更（所有新增符号均不导出：`fieldValue`/`typeMeta`/`ruleTemplate`/`ctxValidatorBuilders`；唯一新增导出 `ResetTypeCache()`，纯增量）
- [x] 基准达成（对照 P0 基线，count=6）
- [x] 覆盖率 95.6%（不降）
- [x] `data_source.go` 838 行（≤1000）

**达标线 vs 实测**（Go 1.25.10，i7-14700KF，count=6）

| 场景 | 目标 | 实测 | 结论 |
|---|---|---|---|
| 扁平 struct | ns↓≥50%，allocs↓≥40% | **-78% ns (10403→2295)，-74% allocs (132→34)** | ✅ 远超 |
| 嵌套 struct | ns↓≥50% | **-79% ns (10128→2141)，-74% allocs** | ✅ 远超 |
| Map 验证 | 持平(±5%) | **-65% ns (9606→3314)，-42% allocs** | ✅ 超预期（受益于 translator+ctx 共享） |
| 首次解析新类型 | ≤基线+15% | **-91% ns (8580→754)，-78% allocs** | ✅ 远超 |
| Slice-of-struct（动态） | —（原计划走原路径） | -37% ns，-20% allocs（受益于构造共享） | ✅ 额外收益 |
| Val/Var | — | 持平（725ns/11），**并发 bug 已修** | ✅ |

> 两个随手修复的真实 bug：①递归 struct 类型 `buildTypeMeta` 栈溢出（P2）；②`Val`/`Var` 共享实例并发 race（P5-deferred）。

**可选剩余**（非 v1.6.0 必需）：
- P4 管线拆分 + 下沉 internal（可维护性，痛点 D/E）
- opt-in `Factory`/pool（heavy-reuse 场景）
- README/README.zh-CN 性能说明
- SliceOfStruct 动态路径每元素优化

---

## 5. 风险与回滚

| 风险 | 阶段 | 缓解 / 回滚 |
|---|---|---|
| build/instantiate 拆分遗漏分支，规则集合不一致 | P3 | `regression_compat_test.go` 守护；不一致即回滚该提交 |
| `FieldByIndex` 对匿名/私有字段 panic | P2 | 保留 `fieldNames` 分类，非导出走原路径；用例覆盖 `ValidatePrivateFields` |
| 共享模板被运行时变异 → 并发串数据 | P3/P5 | 模板构建后只读；`-race` 跑并发基准 |
| internal 迁移引发 import 环 | P4 | 小步提交；难解则 FieldValue/TypeMeta 暂留根包（设计已许可） |
| 池化 Reset 不彻底串数据 | P5 | opt-in、非默认；`-race` + Reset 全字段核对 |

**回滚策略**：每阶段独立提交，出问题回滚到上一阶段末尾的绿色提交即可；不同阶段互不阻塞发版下限——即使只完成到 P3，已具备可发布的性能收益。

---

**文档结束** — v1.6.0 性能核心 + 两个 bug 修复已完成（9 个提交，P0–P3/P5/P6）。P4（可维护性）与 Factory/pool 作为可选项留待后续。
